const crypto=require('crypto');function safeEqual(a,b){const x=Buffer.from(String(a||'')),y=Buffer.from(String(b||''));return x.length===y.length&&x.length>0&&crypto.timingSafeEqual(x,y)}function requireCoreAuth(env=process.env){return(req,res,next)=>{const required=String(env.SUPERVISOR_REQUIRE_AUTH??'true').toLowerCase()!=='false';if(!required)return next();const token=String(env.SUPERVISOR_API_TOKEN||'').trim();if(!token)return res.status(503).json({ok:false,error:'CORE_AUTH_NOT_CONFIGURED'});const supplied=req.get('x-supervisor-token')||'';if(!safeEqual(supplied,token))return res.status(401).json({ok:false,error:'UNAUTHORIZED'});next()}}module.exports={safeEqual,requireCoreAuth};
