const express=require('express');const path=require('path');const {requireCoreAuth}=require('./security');const {validateSources}=require('../core/sourceValidation');const {TelegramAdapter}=require('../adapters/telegramAdapter');const {version}=require('../../package.json');
function createApp({engine,databases,config}){const app=express();app.use(express.json({limit:'1mb'}));const auth=requireCoreAuth(process.env),telegram=new TelegramAdapter(process.env);app.get('/',(_q,r)=>r.sendFile(path.join(__dirname,'../../public/index.html')));app.get('/health',(_q,r)=>r.json({ok:true,version,databases:databases.ids,sourceReadOnly:true,telegramConfigured:telegram.isConfigured()}));app.get('/api/core/validate-sources',auth,async(_q,r)=>r.json({ok:true,validation:await validateSources(databases)}));app.get('/api/core/status',auth,async(_q,r)=>r.json({ok:true,version,latestRun:await engine.store.getLatestRun(),crmInitialSnapshot:await engine.store.getCheckpoint('crm_initial_snapshot_v1')}));app.post('/api/core/run',auth,async(q,r)=>{try{const now=q.body?.now?new Date(q.body.now):new Date();r.json({ok:true,result:await engine.run({now})})}catch(e){r.status(500).json({ok:false,error:e.message})}});app.get('/api/supervisor/report',auth,async(_q,r)=>r.json({ok:true,report:await engine.store.getLatestReport()}));app.post('/api/supervisor/report/send',auth,async(q,r)=>{try{let report=await engine.store.getLatestReport();if(q.body?.run===true||!report){const out=await engine.run({now:new Date()});report=out.report}const sent=await telegram.send(report.text);await engine.store.saveReport(report.id||report.reportId||`manual-${Date.now()}`,{...report,telegramSent:true,telegramMessageId:sent.messageId,telegramSentAt:new Date().toISOString()});r.json({ok:true,sent})}catch(e){r.status(500).json({ok:false,error:e.message})}});return app}module.exports={createApp};
