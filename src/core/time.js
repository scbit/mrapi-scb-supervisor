function asDate(v){if(!v)return null;if(v instanceof Date)return isNaN(v)?null:v;if(typeof v.toDate==='function')return v.toDate();if(v._seconds!==undefined)return new Date(Number(v._seconds)*1000);const d=new Date(v);return isNaN(d)?null:d}
function toIso(v){const d=asDate(v);return d?d.toISOString():null} function minutesBetween(a,b){const x=asDate(a),y=asDate(b);return x&&y?Math.round((y-x)/60000):null}
function localDateParts(date,tz='America/Argentina/Buenos_Aires'){const d=asDate(date)||new Date();const f=new Intl.DateTimeFormat('en-CA',{timeZone:tz,year:'numeric',month:'2-digit',day:'2-digit',weekday:'short',hour:'2-digit',minute:'2-digit',hourCycle:'h23'});const p=Object.fromEntries(f.formatToParts(d).filter(x=>x.type!=='literal').map(x=>[x.type,x.value]));const wm={Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6,Sun:0};return{year:p.year,month:p.month,day:p.day,ymd:`${p.year}-${p.month}-${p.day}`,weekday:wm[p.weekday],hour:Number(p.hour),minute:Number(p.minute)}}
function daysOverdue(due,now=new Date(),tz='America/Argentina/Buenos_Aires'){const a=asDate(due),b=asDate(now);if(!a||!b)return null;const x=localDateParts(a,tz),y=localDateParts(b,tz);const n=Math.floor((Date.UTC(+y.year,+y.month-1,+y.day)-Date.UTC(+x.year,+x.month-1,+x.day))/86400000);return Math.max(0,n)}
function localDayRange(date,tz='America/Argentina/Buenos_Aires'){const p=localDateParts(date,tz);const probe=new Date(`${p.ymd}T12:00:00Z`);const q=localDateParts(probe,tz);const off=(Date.UTC(+q.year,+q.month-1,+q.day,q.hour,q.minute)-probe.getTime())/60000;const start=new Date(Date.UTC(+p.year,+p.month-1,+p.day)-off*60000);return{ymd:p.ymd,start,endExclusive:new Date(start.getTime()+86400000)}}
module.exports={asDate,toIso,minutesBetween,daysOverdue,localDateParts,localDayRange};
