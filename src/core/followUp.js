const {daysOverdue,asDate}=require('./time');const {normalizeStage}=require('./normalizers');
function classifyFollowUp(deal,config,now=new Date()){const stage=normalizeStage(deal.stageNorm||deal.stage);const active=new Set((config.follow_up?.active_stages||[]).map(normalizeStage));if(!active.has(stage)||deal.isClosed)return{tracked:false,dealId:deal.id,stage};const due=asDate(deal.dueDate);if(!due)return{tracked:false,dealId:deal.id,stage};const days=daysOverdue(due,now,config.timezone);if(days===null||due>now)return{tracked:false,dealId:deal.id,stage};let bucket='DUE';if(days>=60)bucket='PLUS_60';else if(days>=30)bucket='PLUS_30';else if(days>=15)bucket='PLUS_15';return{tracked:true,dealId:deal.id,seller:deal.owner||null,client:deal.title||deal.contactId||null,contactId:deal.contactId||null,conversationId:deal.conversationId||null,stage,stageRaw:deal.stage||null,dueDate:deal.dueDate,daysOverdue:days,bucket,lastContactAt:deal.lastContactAt||null,lastRecontactAt:deal.lastRecontactAt||null}}
module.exports={classifyFollowUp};
