const functions=require('@google-cloud/functions-framework');const {loadConfig}=require('./config');const {createDatabases}=require('./adapters/firestoreFactory');const {InboxAdapter}=require('./adapters/inboxAdapter');const {CrmAdapter}=require('./adapters/crmAdapter');const {HunterAdapter}=require('./adapters/hunterAdapter');const {SupervisorStore}=require('./persistence/supervisorStore');const {SupervisorEngine}=require('./core/engine');const {createApp}=require('./http/app');const config=loadConfig();const databases=createDatabases();const engine=new SupervisorEngine({config,inbox:new InboxAdapter(databases.inbox,config),crm:new CrmAdapter(databases.crm,config),hunter:new HunterAdapter(databases.hunter),store:new SupervisorStore(databases.supervisor)});const app=createApp({engine,databases,config});functions.http('helloHttp',app);module.exports={app,engine};
