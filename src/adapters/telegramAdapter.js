class TelegramAdapter{constructor(env=process.env,fetchImpl=global.fetch){this.token=String(env.TELEGRAM_BOT_TOKEN||'').trim();this.chatId=String(env.TELEGRAM_CHAT_ID||'').trim();this.fetch=fetchImpl}isConfigured(){return!!(this.token&&this.chatId)}async send(text){if(!this.isConfigured())throw new Error('TELEGRAM_NOT_CONFIGURED');const url=`https://api.telegram.org/bot${this.token}/sendMessage`;const r=await this.fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:this.chatId,text:String(text||''),disable_web_page_preview:true})});const data=await r.json();if(!r.ok||!data.ok)throw new Error(`TELEGRAM_SEND_FAILED:${data.description||r.status}`);return{ok:true,messageId:data.result?.message_id||null,chatId:this.chatId}}
}
module.exports={TelegramAdapter};
