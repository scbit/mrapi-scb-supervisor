const {normalizeConversation,normalizeMessage}=require('../core/normalizers');
class InboxAdapter{constructor(db,config){this.db=db;this.config=config}async listChangedConversations({since,limit}){const max=Math.max(1,Number(limit||this.config.incremental.max_conversations_per_run||250));let q=this.db.collection('conversations').orderBy('lastMessageAt','asc');if(since)q=q.where('lastMessageAt','>=',since);const s=await q.limit(max).get();return s.docs.map(d=>normalizeConversation(d.id,d.data()))}async getConversation(id){const d=await this.db.collection('conversations').doc(String(id)).get();return d.exists?normalizeConversation(d.id,d.data()):null}async getMessages(id,limit){const max=Math.max(1,Number(limit||500));let s;try{s=await this.db.collection(`conversations/${id}/messages`).orderBy('timestamp','asc').limit(max).get()}catch(_){s=await this.db.collection(`conversations/${id}/messages`).limit(max).get()}return s.docs.map(d=>normalizeMessage(d.id,d.data())).sort((a,b)=>new Date(a.timestamp||0)-new Date(b.timestamp||0))}}
module.exports={InboxAdapter};
